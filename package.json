{
  "name": "vibe_flow_ppt",
  "version": "0.2.0",
  "description": "Marp CLI persistent worker for HTML presentation rendering",
  "private": true,
  "dependencies": {
    "@marp-team/marp-cli": "^4.1.1"
  },
  "scripts": {
    "marp": "marp",
    "worker": "node scripts/marp_worker.js"
  }
}
